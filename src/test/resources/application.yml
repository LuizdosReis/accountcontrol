spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/controle_orcamento_test
    username: user_controle_test
    password: controle_test

  jpa:
    properties:
      hibernate:
        temp:
          use_jdbc_metadata_defaults: false